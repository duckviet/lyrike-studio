export const MIN_LINE_LENGTH_SEC = 0.05;
export const NUDGE_DELTA_SEC = 0.1;
export const DRAG_DRIFT_THRESHOLD_SEC = 0.2;

/** Gaps narrower than this (in px) won't render a gap region — too small to target. */
export const MIN_GAP_PX = 4;

/** Gaps narrower than this (in px) render as a thin dash only, no popup. */
export const MIN_GAP_INTERACTIVE_PX = 24;
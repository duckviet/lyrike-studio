import { MediaController, WaveformController } from "@/entities/media";

// Singleton controllers for the editor
export const editorMediaController = new MediaController();
export const editorWaveformController = new WaveformController();
